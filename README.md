## How to run
1. Clone this repository.

2. Open ```index.html``` in browser.

## API
```javascript
let othello = new Othello({
        canvas, 
        playerColor,
        playerScoreEl,
        botScoreEl,
        highScoreEl
    });
othello.render()
```
